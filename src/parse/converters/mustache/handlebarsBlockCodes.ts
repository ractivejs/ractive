import TemplateItemType from 'config/types';

export default {
  each: TemplateItemType.SECTION_EACH,
  if: TemplateItemType.SECTION_IF,
  with: TemplateItemType.SECTION_IF_WITH,
  unless: TemplateItemType.SECTION_UNLESS
};
