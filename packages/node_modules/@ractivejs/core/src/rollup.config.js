import pkg from '../package.json'
import buble from 'rollup-plugin-buble'

export default {
  plugins: [
    buble({ transforms: { modules: false } })
  ],
  input: 'src/index.js',
  output: [
    { sourcemap: true, file: pkg.main, format: 'umd', name: 'Ractive' },
    { sourcemap: true, file: pkg.module, format: 'es' }
  ]
}
