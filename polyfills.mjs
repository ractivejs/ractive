// TODO: remove this once 1.0 lands because it might be referenced in the wild for 0.9
console.log( 'You no longer need to include the Ractive polyfills, as everything for IE9+ is now included in the main build.' ); // eslint-disable-line no-console
